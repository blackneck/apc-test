import Icon from 'react-native-vector-icons/Octicons';

import {appColors} from 'src/common/colors';

export const bookmark = Icon.getImageSourceSync('bookmark', 24, appColors.blue);
export const plus = Icon.getImageSourceSync('plus', 24, appColors.blue);
export const trashcan = Icon.getImageSourceSync('trashcan', 24, appColors.red);
export const searchTabIcon = Icon.getImageSourceSync('search', 20);
export const bookmarkTabIcon = Icon.getImageSourceSync('bookmark', 20);
